!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'Bloom Group Devise_Layout'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'Bloom Group'}"}
    = stylesheet_link_tag "application"
    -# Foundation 5 won't work with Rails Turbolinks
    -# Modernizr is required for Zurb Foundation
    = javascript_include_tag "vendor/modernizr"
    = csrf_meta_tags
  %body{:style=>"width:50%;margin:auto"}
    %header
    %main{:role => "main"}
      = render 'layouts/messages'
      = yield
    -# Foundation 5 won't work when application.js is included in HEAD
    = javascript_include_tag "application"
